# AGENTS

仓库级 agent 入口文件。
所有的文档和注释都使用中文，打印到控制台的提示未特殊声明都用英文。

## 约定rules
- `CLAUDE.md` 必须是指向本文件 `AGENTS.md` 的软链接。
- `AGENTS.md` 保持精简。
- `.state/`存放agents工作时候的状态文件，从其中恢复工作状态；每次执行完都要更新`.state/`的文件来对应agent工作状态
- 时刻检查文档、注释中是否有语义冲突的，代码中有含义、逻辑冲突的
- 不许删去`// I AM NOT DONE`这一行